# API 변경 분석 — 2026-08-24

- 대상 커밋: `1a22e3cea19c88903f6f3bbf7e3d3000186f37b5` (`docs(migration): openapi-snapshot 자동 갱신`)
- spec version: `1.2.14 → 1.2.14` (버전 번호 변화 없음, 내용만 갱신)
- paths: 33 → 33 (변화 없음) / schemas: 90 → 90 (변화 없음)
- `docs/migration/asyncapi.latest.json` (실시간 웹소켓 스펙)은 이번 커밋에서 변경되지 않음.
- `docs/reverse-engineering/wts-endpoints.json` (WTS 카탈로그, wts-monitor 봇 주간 갱신)도
  최근 26시간 내 갱신됨 — 2절에서 별도로 다룬다.

## 1. 공식 Open API — `timeInForce` : 국내(KR) 주식 시가단일가(OPG) 주문 정식 지원

`tools/openapi_diff.py` 결과, 신규/삭제된 경로·스키마는 없고 필드 설명(description) 변경만 발생했다.
`[미분류 변경]` 절도 같은 필드의 부수 변경(enum 배열 값 추가)이라 별도 조사로 확인 완료 — 미확인 잔여 없음.

1. **`Order.timeInForce` (응답 스키마) description**
   - 기존: `OPG`: 장 개시 주문 (At the Opening). **현재는 지원하지 않습니다.**
   - 변경 후: `OPG`: 장 개시 주문 (At the Opening, **국내 시가단일가**).
   - enum 값 자체(`DAY`/`CLS`/`OPG`)는 이전부터 동일 — 응답 스키마는 이미 `OPG` 를 문서화해 두었으나 "미지원" 각주만 있었음.

2. **`OrderCreateRequest.oneOf[0].properties.timeInForce` (요청 스키마, 수량 기반 주문)**
   - `enum`: `["DAY", "CLS"]` → `["DAY", "CLS", "OPG"]` (요청 바디에 `OPG` 사용 가능해짐)
   - description 에 조건 추가: *"현재 국내 주식 전용이며 `orderType` 은 `LIMIT`/`MARKET` 모두 지원합니다. 세션 시간(장전 사전접수) 외 접수는 원장에서 거절될 수 있습니다."*

즉 이번 변경은 신규 필드/엔드포인트가 아니라, 이전부터 응답 스키마에 예약되어 있던 `OPG` 값을 **요청 스키마에서도 실제로 허용**하도록 활성화한 것이다 (국내 주식, `LIMIT`/`MARKET` 공통, 장전 시간외 접수는 원장 거절 가능).

### tossctl 커버리지 대조

- `internal/orderintent/`: `TimeInForce` 개념 자체가 존재하지 않음 (grep 결과 없음).
- `internal/official/orders_write.go`:
  - `orderCreateV0.TimeInForce` 필드는 존재하지만, `buildOrderCreate()` 에서 **`orderType == "LIMIT"` 일 때 항상 `"DAY"` 로 하드코딩** (`orders_write.go:134`).
  - `CLS`/`OPG` 를 보낼 경로가 코드 어디에도 없음 — CLI 는 이 값들을 절대 전송하지 않는다.
  - `internal/mcp/operations.go` 는 이 저장소에 존재하지 않는다 (확인 완료, MCP 서버 레이어 없음) — 별도 확인 불필요.
- 결론: 현재 tossctl 구현은 이번 스펙 변경의 영향을 받지 않는다 (호출 안 함 → 깨질 것도 없음). 다만 국내 시가단일가 주문 기능을 노출하지 않고 있으므로 **기능 격차(gap)** 로 볼 수 있다.

### 분류 — **(b) 신규 기능 후보**

- Breaking 위험 없음: 기존 요청/응답 필드 삭제나 타입 변경이 아니고, CLI 는 애초에 `DAY` 외 값을 보내지 않으므로 하위 호환성 문제가 발생하지 않는다.
- 신규로 열린 기능: 국내 주식 시가단일가(장 개시, OPG) 주문을 `LIMIT`/`MARKET` 모두로 낼 수 있게 됨.

## 2. WTS 카탈로그 — 추출기 정확도 개선이 대부분, 실사용 영향 없음

`docs/reverse-engineering/wts-endpoints.json` 은 wts-monitor 봇 커밋(`0c89f25`, 08-24 02:18 UTC)
으로 갱신된 뒤, 같은 26시간 창 안에서 사람이 커밋 4건(`5228150` `3a91a63` `7a7f9b6` `371cdec`)
으로 이미 대응·재생성했다. 그중 `5228150` 의 커밋 메시지가 재구조화의 핵심 원인을 밝힌다:

> 카탈로그 추출기가 경로만 정규식으로 긁던 것을 번들의 host/method/path 삼중 정의를 읽도록
> 바꿨다. 기존 방식은 `[` 에서 멈춰 동적 세그먼트를 잘랐고(85건), 프로버가 그 잘린 경로를
> 때려 33건을 위양성 404 로 기각했다.

즉 봇 커밋 직후 **카탈로그 추출기 자체를 고쳤고**, 그 결과로 카탈로그가 재구조화됐다. 봇
커밋 직후 시점(추출기 수정 전)과 현재 main 을 구조적으로 비교하면:

| 지표 | 이전 | 이후 |
|---|---|---|
| 총 경로 | 1005 | 1087 |
| implemented | 112 | 119 |
| candidate | 377 | 332 |
| excluded | 516 | 636 |
| 키 추가 | — | 161 |
| 키 삭제 | — | 79 |

키 79건 "삭제"는 대부분 진짜 삭제가 아니라 **파라미터 없는 잘린 경로가 `{param}` 포함
정확한 경로로 재키잉**된 것이다(예: `/api/v1/exchange/usd/base-exchange-rate` →
`/…/base-exchange-rate/{date}`, `/api/v1/my-assets/summaries/markets` →
`/…/markets/{market}/withdrawable-amount` 등). "카탈로그의 삭제 = 백엔드 폐지" 가 아니라는
전제가 이번엔 정확히 들어맞는다.

### 실사용(breaking) 여부 확인

이전 카탈로그에서 `status: implemented` 였던 9개 경로가 이번에 "삭제"됐다. 각각을
`internal/client/` 에서 grep 해 재키잉된 경로와 대조:

| 사라진 구 경로 | 실제 코드 (internal/client/) | 판정 |
|---|---|---|
| `exchange/usd/base-exchange-rate` | `trading.go:585` — **파라미터 없이** 그대로 호출 | ⚠️ 아래 참고 |
| `my-assets/summaries/markets` | `account.go:108,117,121` — 이미 `{market}/withdrawable-amount`, `all/overview` 등 정확한 경로 사용 중 | 영향 없음 |
| `my-assets/transactions/markets` | `transactions.go:145,311` — 이미 `{market}` 파라미터 포함 | 영향 없음 |
| `trading/my-orders/markets` (v2) | `completed_orders.go:44` — 이미 `{market}/by-date/completed` | 영향 없음 |
| `reasoning/stocks` (v2) | `insights.go:45` — 이미 `{stockCode}` 포함 | 영향 없음 |
| `stock-infos/header` | 미사용 확인 (candidate 상태 유지) | 영향 없음 |
| `growth/autotrade/plan/stock` | `accumulation.go:92` — 이미 `+ productCode` 로 조합 | 영향 없음 |
| `new-watchlists/suggest` | 미사용 | 영향 없음 |

8/9 은 우리 코드가 원래부터 정확한 파라미터화 경로를 쓰고 있어 카탈로그 재키잉과 무관하다.

**예외 1건**: `internal/client/trading.go:585` 의 `getUSDBaseExchangeRate` 는
`/api/v1/exchange/usd/base-exchange-rate` 를 **경로 파라미터 없이** 호출한다. 새 카탈로그는
이 경로를 `{date}` 필수 세그먼트가 있는 것으로만 인식한다 — 구 카탈로그의 파라미터 없는
버전은 추출기가 `[date]` 를 자르고 남긴 오탐이었다는 게 `5228150` 커밋 메시지의 명시적
설명이다. 다만:
- 이 코드는 이번 26시간 변경으로 새로 생긴 게 아니라 기존 구현이고, 지금까지 정상 동작해
  온 것으로 보인다(원장/게이트웨이가 트레일링 파라미터 생략을 "최신값"으로 처리할 가능성).
- 라이브 호출이 금지된 분석 세션이라 직접 검증은 못 했다.
- **breaking 이 새로 발생했다는 근거는 없지만, 카탈로그 추출기가 이제 이 경로를 "파라미터
  필수"로 더 정확하게 보고 있다는 점은 사람이 한 번 라이브로 확인해볼 가치가 있다.**

### 신규 후보 — asset-snapshot all-accounts 계열 (진짜 신규, 08-24 첫 관측)

재키잉이 아니라 `first_seen: 2026-08-24` 로 진짜 새로 잡힌 경로 3건:

- `GET /api/v1/asset-snapshot/all-accounts/chart/{range}/{stepUnit}`
- `GET /api/v1/asset-snapshot/all-accounts/detail-by-date`
- `GET /api/v1/asset-snapshot/all-accounts/page`

기존 `asset-snapshot/*`(계좌 단위) 의 **전 계좌 합산 버전**. 카탈로그 노트에 이미
"웹 UI 없음 확정(2026-08-24 라이브 캡처) — 모바일 전용" 으로 기록돼 있다 — 사람이 이미
라이브로 확인하고 후보 상태로만 남겨둔 것. 웹 UI 트리거가 없어 파라미터(특히 `range`/
`stepUnit` 어휘)를 추가로 관측하기 전까지는 개발 우선순위 낮음.

### 이미 구현 완료 확인 (조치 불필요)

봇 커밋 이후 사람이 같은 창에서 실제로 반응한 기능 4건은 카탈로그에도 `implemented` 로
반영돼 있다 — 이번 분석에서 새로 띄울 필요 없음.

| 기능 | 커밋 | 카탈로그 경로 |
|---|---|---|
| 다종목 당일 분봉 일괄 조회 (`quote charts`) | `371cdec` | `dashboard/common/stocks/mini-chart` |
| 지수 이상 움직임 · 배치 AI 사유 | `7a7f9b6` | `dashboard/wts/overview/ai-signals`, `v4/dashboard/wts/overview/indicator` |
| 프라임 누적 혜택 | `7a7f9b6` | `prime/users/benefits/cumulative` |
| 서킷브레이커·사이드카 발동 상태 (`market halt`) | `5228150` | `dashboard/common/market-event/circuit-breaker` |

### 분류

| 변경 | 분류 |
|---|---|
| WTS 카탈로그 재키잉 79건 (파라미터 없는 경로 → `{param}` 포함 경로) | (a) no-op — 코드는 이미 정확한 경로 사용 중 (1건 예외, 위 참고) |
| `exchange/usd/base-exchange-rate` 파라미터 미포함 호출 | 경계 사례 — 이번 diff 가 만든 문제는 아니나, 추출기 정확도 개선으로 처음 드러난 기존 구현의 잠재 리스크. **(c) 로 올리진 않음** (신규 breaking 증거 없음, 라이브 미검증) |
| `asset-snapshot/all-accounts/*` 3건 신규 관측 | (b) 신규 기능 후보 (우선순위 낮음 — 파라미터 어휘 미상, 웹 UI 트리거 없음) |
| WTS 신규 기능 4건 (분봉/AI 사유/프라임 혜택/서킷브레이커) | (a) no-op — 이미 구현 완료 |

## 3. 권장 후속 조치

1. `timeInForce=OPG` 지원 여부 결정. `orderintent.PlaceIntent` 에 `TimeInForce` 필드를 추가하고
   `--time-in-force`(가칭) CLI 플래그로 `DAY`/`CLS`/`OPG` 중 선택할 수 있게 확장하는 것을 검토한다.
   - 현재는 `LIMIT` 주문에서만 `TimeInForce` 를 보내므로, `OPG`+`MARKET` 조합을 지원하려면
     `buildOrderCreate()` 의 `if orderType == "LIMIT"` 분기 밖에서도 `TimeInForce` 를 세팅하도록
     로직을 손봐야 한다.
   - `OPG` 는 국내 주식 전용이므로 해외(US) 심볼에 대해서는 거부하거나 무시하는 가드가 필요하다.
   - 세션 시간(장전 사전접수) 외 접수 시 원장에서 거절될 수 있다는 문서 경고를 사용자 안내
     문구에 반영할지 판단 필요.
   - 즉시 조치는 불필요 (no-op 아님이지만 breaking 도 아님) — 다음 기능 스프린트에서 우선순위 판단.
2. `getUSDBaseExchangeRate` 가 실제로 `{date}` 없이 최신값을 반환하는지 라이브로 한 번
   확인. 문제없으면 그대로 두되, 카탈로그 노트에 "확인됨" 기록 권장.
3. `asset-snapshot/all-accounts/*` 는 파라미터 어휘(특히 `range`/`stepUnit`) 관측 전까지
   보류 — 트리거 조건 없음.
